const READERS = __READERS__;
const FILES = __FILES__;

const css = `
:root{--ink:#203337;--muted:#5e6e6b;--paper:#f6f2e9;--canvas:#e5ece7;--green:#214f49;--orange:#cf6f45;--line:#d3d8cf;--card:#fffdf8}*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;background:var(--canvas);color:var(--ink);font-family:Tahoma,"Geeza Pro",Arial,sans-serif}a{color:inherit;text-decoration:none}.site-header{max-width:1140px;margin:auto;padding:24px 32px;display:flex;align-items:center;justify-content:space-between;direction:rtl}.wordmark{color:var(--green);font-family:Georgia,Tahoma,serif;font-size:1.1rem;font-weight:800}.site-header nav{display:flex;gap:22px;color:var(--muted);font-size:.9rem}.site-header nav a:hover,.back-link:hover{color:var(--orange)}.hero{max-width:1140px;margin:28px auto 0;padding:70px 32px 78px;background:var(--green);color:#fffdf6;text-align:right}.eyebrow{margin:0 0 18px;color:#f6bb89;font-size:.85rem;font-weight:800;letter-spacing:.08em}.hero h1{margin:0;font-family:Georgia,Tahoma,serif;font-size:clamp(2.5rem,7vw,5.8rem);line-height:1.05}.hero-copy{max-width:700px;margin:24px 0 0 auto;font-size:1.12rem;line-height:2;color:#e8eee8}.stats{margin-top:42px;display:flex;flex-wrap:wrap;gap:8px;direction:ltr;font-family:Arial,sans-serif;font-size:.75rem}.stats span{padding:7px 10px;border:1px solid rgba(255,255,255,.28);border-radius:999px}.reading-note{max-width:960px;margin:-32px auto 0;padding:24px 30px;display:flex;gap:18px;align-items:flex-start;background:#f6bb89;color:#3d352b;box-shadow:0 12px 30px rgba(26,45,39,.12)}.reading-note p{margin:0;line-height:1.95}.note-mark{flex:0 0 auto;width:30px;height:30px;border-radius:50%;display:grid;place-items:center;background:var(--green);color:white;font-weight:800}.collections{max-width:1140px;margin:80px auto;padding:0 32px}.collection{margin-bottom:72px}.collection-heading{display:flex;justify-content:space-between;align-items:baseline;padding-bottom:15px;border-bottom:2px solid var(--green)}.collection-heading p{margin:0;font-size:1.45rem;font-weight:800;color:var(--green)}.collection-heading span{color:var(--muted);font-size:.85rem}.reader-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:16px;margin-top:20px}.reader-card{min-height:246px;padding:22px;display:flex;flex-direction:column;background:var(--card);border:1px solid var(--line);box-shadow:0 10px 22px rgba(40,58,50,.06)}.reader-card-top{display:flex;justify-content:space-between;color:var(--muted);font-size:.77rem}.reader-card-top p{margin:0;color:var(--orange);font-weight:800}.reader-card h2{margin:26px 0 8px;color:var(--green);font-family:Georgia,Tahoma,serif;font-size:1.35rem;line-height:1.6}.english-title{margin:0;direction:ltr;text-align:left;color:var(--muted);font:.82rem/1.5 Arial,sans-serif}.card-actions{margin-top:auto;display:flex;flex-wrap:wrap;justify-content:space-between;gap:9px;align-items:center}.read-button{background:var(--green);color:white;padding:9px 14px;font-size:.86rem;font-weight:800}.downloads{display:flex;gap:10px;color:var(--orange);font-size:.82rem}.downloads a:hover{text-decoration:underline}footer{max-width:1140px;margin:auto;padding:30px 32px 50px;color:var(--muted);border-top:1px solid var(--line);text-align:center}.reader-page{max-width:940px;margin:auto;padding:28px 32px 74px}.reader-header{display:grid;grid-template-columns:1fr auto;gap:14px;align-items:end;padding-bottom:22px;border-bottom:2px solid var(--green)}.back-link{grid-column:1/-1;justify-self:start;color:var(--muted);font-size:.85rem}.reader-title-block p{margin:0 0 7px;color:var(--orange);font-size:.82rem;font-weight:800}.reader-title-block h1{margin:0;color:var(--green);font:800 clamp(1.7rem,5vw,3rem)/1.3 Georgia,Tahoma,serif}.reader-title-block span{display:block;margin-top:5px;color:var(--muted);font:.86rem Arial,sans-serif;text-align:right}.week-chip{align-self:center;padding:7px 10px;background:#f6bb89;color:#473828;font-weight:800;white-space:nowrap}.edition-switch{margin:24px 0;display:flex;gap:8px}.edition-switch a{padding:8px 12px;border:1px solid var(--line);background:#eef1ed;color:var(--muted);font-size:.86rem}.edition-switch a.active{border-color:var(--green);background:var(--green);color:white}.week-reader{background:var(--paper);padding:clamp(22px,5vw,56px);box-shadow:0 16px 36px rgba(33,54,47,.1)}.week-intro{display:flex;justify-content:space-between;gap:18px;margin-bottom:28px;color:var(--muted);font-size:.85rem}.week-intro p{margin:0}.source-lines{margin:0;padding:0;list-style:none}.source-lines li{margin:0 0 16px}.source-line{margin:0;font-family:Georgia,Tahoma,serif;font-size:clamp(1.08rem,2.8vw,1.38rem);line-height:2.05}.source-line span{display:inline-block;margin-left:.75rem;color:var(--orange);font:700 .72rem Arial,sans-serif;direction:ltr}.gloss{margin:1px 0 0;padding-left:18px;color:var(--muted);font:.82rem/1.45 Arial,sans-serif;text-align:left}.teacher-notes{margin-top:32px;padding-top:22px;border-top:1px solid var(--line);color:#31413d;font-family:Arial,sans-serif}.teacher-notes h2{margin:0 0 10px;color:var(--green);font-size:1rem}.teacher-notes ul{margin:0;padding-left:17px;font-size:.86rem;line-height:1.65}.teacher-notes p{margin:14px 0 0;color:var(--muted);font-size:.84rem;line-height:1.6}.week-navigation{display:grid;grid-template-columns:1fr auto 1fr;align-items:center;gap:16px;margin:30px 0;color:var(--muted);font-size:.85rem}.week-navigation a{color:var(--green);font-weight:800}.week-navigation a:last-child{justify-self:end}.reader-downloads{padding:20px;background:#dae5de}.reader-downloads p{margin:0 0 11px;color:var(--green);font-weight:800}.reader-downloads a{display:inline-block;margin-left:8px;padding:8px 11px;border:1px solid var(--green);color:var(--green);font-size:.86rem}.reader-downloads a:hover{background:var(--green);color:white}@media(max-width:720px){.site-header{padding:20px}.site-header nav{gap:12px;font-size:.8rem}.hero{margin-top:0;padding:58px 22px 68px}.reading-note{margin:-22px 20px 0;padding:20px}.collections{padding:0 20px;margin:62px auto}.reader-grid{grid-template-columns:1fr}.reader-card{min-height:212px}.reader-page{padding:20px 16px 58px}.reader-header{grid-template-columns:1fr}.week-chip{justify-self:start}.week-reader{padding:22px 18px}.source-line{line-height:1.9}}
body.dark{--ink:#edf6f3;--muted:#b7c6c2;--paper:#17211f;--canvas:#0d1312;--green:#a3d3bd;--orange:#f6b27f;--line:#3b504b;--card:#14201e}body.dark .hero{background:#183b34;color:#f4fbf7}body.dark .hero-copy{color:#d8e8e1}body.dark .reading-note{background:#3d3024;color:#f6e9dc}body.dark .note-mark{background:#7eb89e;color:#10221c}body.dark .read-button,body.dark .edition-switch a.active{background:#558b74;color:#fff}body.dark .edition-switch a{background:#1e2d2a;color:var(--muted)}body.dark .reader-downloads{background:#20342f}body.dark .teacher-notes{color:#e0ece7}body.dark .week-chip{background:#9e6e4a;color:#fff}body.dark .week-navigation select{background:#17211f;color:var(--ink);border-color:var(--green)}.theme-toggle{position:fixed;z-index:5;left:18px;top:18px;width:40px;height:40px;border:1px solid var(--line);border-radius:50%;background:var(--card);color:var(--ink);font:1.05rem Arial;cursor:pointer;box-shadow:0 4px 14px rgba(0,0,0,.14)}.theme-toggle:hover{border-color:var(--orange)}
`;

const faNumber = (value) => new Intl.NumberFormat("fa-IR").format(value);
const escape = (value) => String(value).replace(/[&<>"']/g, (character) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#039;" })[character]);
const page = (title, content) => `<!doctype html><html lang="fa" dir="rtl"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${escape(title)}</title><style>${css}</style></head><body><button class="theme-toggle" type="button" aria-label="تغییر حالت روشن و تیره" title="حالت روشن و تیره">◐</button>${content}<script>const toggle=document.querySelector('.theme-toggle');const saved=localStorage.getItem('farsi-theme');if(saved==='dark'||(!saved&&matchMedia('(prefers-color-scheme: dark)').matches))document.body.classList.add('dark');toggle.addEventListener('click',()=>{document.body.classList.toggle('dark');localStorage.setItem('farsi-theme',document.body.classList.contains('dark')?'dark':'light')});</script></body></html>`;

function home() {
  const categoryOrder = ["علوم و فناوری", "شاهنامه", "شعر", "نثر"];
  const categories = categoryOrder.filter((category) => READERS.some((reader) => reader.category === category));
  const groups = categories.map((category) => ({ category, readers: READERS.filter((reader) => reader.category === category) }));
  const weeklyCount = READERS.reduce((total, reader) => total + reader.weeks.length, 0);
  const cards = groups.map(({ category, readers }) => `<section class="collection" id="${encodeURIComponent(category)}"><div class="collection-heading"><p>${category}</p><span>${faNumber(readers.length)} عنوان</span></div><div class="reader-grid">${readers.map((reader) => `<article class="reader-card"><div class="reader-card-top"><p>${reader.category}</p><span>${faNumber(reader.weeks.length)} هفته</span></div><h2>${reader.title_fa}</h2><p class="english-title">${escape(reader.title_en)}</p><div class="card-actions"><a class="read-button" href="/read/${reader.slug}/1">خواندن متن</a>${reader.student && reader.teacher ? `<div class="downloads"><a href="/downloads/${reader.student}">دانشجو</a><a href="/downloads/${reader.teacher}">آموزگار</a></div>` : `<span class="downloads">خوانش برخط</span>`}</div></article>`).join("")}</div></section>`).join("");
  return page("مواد خوانش فارسی", `<main><header class="site-header"><a class="wordmark" href="/">خوانش فارسی</a><nav>${categories.map((category) => `<a href="#${encodeURIComponent(category)}">${category}</a>`).join("")}</nav></header><section class="hero"><p class="eyebrow">کتابخانهٔ هفتگی</p><h1>موادِ خوانش فارسی</h1><p class="hero-copy">متن‌های فارسی، خط‌به‌خط و آماده برای یک ساعت مطالعهٔ گروهی. هر خوانش در صفحه‌های هفتگی پیش می‌رود و نسخهٔ دانشجو و آموزگار دارد.</p><div class="stats"><span>${faNumber(READERS.length)} عنوان</span><span>${faNumber(weeklyCount)} صفحهٔ هفتگی</span><span>دانشجو و آموزگار</span></div></section><section class="reading-note"><span class="note-mark">۱</span><p>در هر عنوان، «خواندن متن» شما را به هفتهٔ نخست می‌برد. با دکمه‌های هفتهٔ پیش و بعد یا برگزینِ هفته، خوانش گروه را ادامه دهید؛ فایل‌های کامل یا خوانش برخط در دسترس‌اند.</p></section><div class="collections">${cards}</div><footer>متنی برای خواندنِ آرام، گفت‌وگو، و ادامه دادن در هفتهٔ بعد.</footer></main>`);
}

function readerPage(reader, week, teacher) {
  const lines = week.lines.map((line) => `<li><p class="source-line"><span>${line.id}</span>${line.text}</p>${teacher && line.gloss ? `<p class="gloss" dir="ltr">${escape(line.gloss)}</p>` : ""}</li>`).join("");
  const previous = week.number > 1 ? `<a href="/read/${reader.slug}/${week.number - 1}${teacher ? "?edition=teacher" : ""}">هفتهٔ پیش</a>` : "<span></span>";
  const next = week.number < reader.weeks.length ? `<a href="/read/${reader.slug}/${week.number + 1}${teacher ? "?edition=teacher" : ""}">هفتهٔ بعد</a>` : "<span></span>";
  const notes = teacher && week.vocab.length ? `<section class="teacher-notes" dir="ltr"><h2>Vocabulary and older usage</h2><ul>${week.vocab.map((word) => `<li>${escape(word)}</li>`).join("")}</ul>${week.note ? `<p><strong>Teaching note:</strong> ${escape(week.note)}</p>` : ""}</section>` : "";
  const picker = `<label>هفتهٔ <select aria-label="انتخاب هفته" onchange="window.location.href='/read/${reader.slug}/'+this.value+'${teacher ? "?edition=teacher" : ""}'">${reader.weeks.map((item) => `<option value="${item.number}"${item.number === week.number ? " selected" : ""}>${faNumber(item.number)} از ${faNumber(reader.weeks.length)}</option>`).join("")}</select></label>`;
  const downloads = reader.student && reader.teacher ? `<section class="reader-downloads"><p>دریافت نسخهٔ کامل</p><a href="/downloads/${reader.student}">فایل دانشجو</a><a href="/downloads/${reader.teacher}">فایل آموزگار</a></section>` : "";
  return page(`${reader.title_fa} — هفتهٔ ${faNumber(week.number)}`, `<main class="reader-page"><header class="reader-header"><a class="back-link" href="/">← همهٔ خوانش‌ها</a><div class="reader-title-block"><p>${reader.category}</p><h1>${reader.title_fa}</h1>${teacher ? `<span dir="ltr">${escape(reader.title_en)}</span>` : ""}</div><span class="week-chip">هفتهٔ ${faNumber(week.number)}</span></header><nav class="edition-switch"><a class="${teacher ? "" : "active"}" href="/read/${reader.slug}/${week.number}">نسخهٔ دانشجو</a><a class="${teacher ? "active" : ""}" href="/read/${reader.slug}/${week.number}?edition=teacher">نسخهٔ آموزگار</a></nav><article class="week-reader"><div class="week-intro"><p>${week.section_fa}</p><span>${faNumber(week.lines.length)} خط</span></div><ol class="source-lines">${lines}</ol>${notes}</article><nav class="week-navigation">${previous}${picker}${next}</nav>${downloads}</main>`);
}

function download(name) {
  const data = FILES[name];
  if (!data) return new Response("Not found", { status: 404 });
  const binary = atob(data);
  const bytes = new Uint8Array(binary.length);
  for (let index = 0; index < binary.length; index += 1) bytes[index] = binary.charCodeAt(index);
  return new Response(bytes, { headers: { "content-type": "application/vnd.openxmlformats-officedocument.wordprocessingml.document", "content-disposition": `attachment; filename="${name}"` } });
}

export default {
  async fetch(request) {
    const url = new URL(request.url);
    const path = url.pathname.split("/").filter(Boolean).map(decodeURIComponent);
    if (path[0] === "downloads" && path.length === 2) return download(path[1]);
    if (path[0] === "read" && path.length === 3) {
      const reader = READERS.find((item) => item.slug === path[1]);
      const week = reader?.weeks.find((item) => item.number === Number(path[2]));
      if (!reader || !week) return new Response(page("پیدا نشد", "<main class=reader-page><p>این صفحه پیدا نشد.</p></main>"), { status: 404, headers: { "content-type": "text/html; charset=utf-8" } });
      return new Response(readerPage(reader, week, url.searchParams.get("edition") === "teacher"), { headers: { "content-type": "text/html; charset=utf-8" } });
    }
    if (path.length === 0) return new Response(home(), { headers: { "content-type": "text/html; charset=utf-8" } });
    return new Response("Not found", { status: 404 });
  },
};
